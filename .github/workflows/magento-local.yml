name: 🛒 Magento 2 CI/CD

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Check PHP
        run: /usr/bin/php -v

      - name: Composer Install
        run: /home/icodebees/bin/composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader --working-dir=/var/www/html/magento2

      - name: Magento Deploy
        run: |
          cd /var/www/html/magento2
          /usr/bin/php bin/magento maintenance:enable
          /usr/bin/php bin/magento setup:upgrade --keep-generated
          /usr/bin/php bin/magento setup:di:compile
          /usr/bin/php bin/magento setup:static-content:deploy -f en_US
          /usr/bin/php bin/magento cache:flush
          /usr/bin/php bin/magento maintenance:disable

      - name: Fix Permissions
        run: sudo chown -R www-data:www-data /var/www/html/magento2

      - name: Health Check
        run: curl -o /dev/null -s -w "HTTP Status:%{http_code}\n" http://localhost
